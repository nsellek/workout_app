<%= turbo_frame_tag 'modal' do %>
  <%= render 'clients/workout_sets/form',
              set: @workout_set,
              exercise: @exercise,
              url_obj: [:clients, :exercise, @workout_set]
  %>
<% end %>
